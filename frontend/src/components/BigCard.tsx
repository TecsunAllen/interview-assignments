import React from "react";


type Props = {
    title?: string,
    desc: string,
    icon?: number,
}

const BigCard: React.FunctionComponent<Props> = (props): JSX.Element => {
    return <div className="App">{/* write your component here */}</div>;
}

export default BigCard;
